import { IAsset } from './Asset';
import { IAssetPair } from './AssetPair';
export interface IOrderPrice {
    toTokens(): string;
    toMatcherCoins(): string;
    toJSON(): object;
    toString(): string;
}
declare const _default: {
    fromTokens(tokens: any, pair: string | IAsset | IAssetPair, secondAsset?: string | IAsset): Promise<IOrderPrice>;
    fromMatcherCoins(coins: any, pair: string | IAsset | IAssetPair, secondAsset?: string | IAsset): Promise<IOrderPrice>;
    isOrderPrice(object: any): boolean;
};
export default _default;
