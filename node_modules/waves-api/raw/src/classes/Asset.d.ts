import { IAssetObject, IHash } from '../../interfaces';
export interface IAsset extends IAssetObject {
    rating: number;
    ticker: string;
}
declare const _default: {
    get(input: string | IAssetObject): Promise<any>;
    getKnownAssets(): Promise<IHash<any>>;
    getKnownAssetsList(): Promise<any[]>;
    clearCache(): Promise<void>;
    isAsset(object: any): boolean;
};
export default _default;
